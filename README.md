# HU eLearning Frontend

Next.js frontend for the rebuilt HU eLearning platform.

## Local development

1. Copy `.env.local.example` to `.env.local`
2. Set `NEXT_PUBLIC_API_BASE_URL` to the backend API URL
3. Run `npm install`
4. Run `npm run dev`
